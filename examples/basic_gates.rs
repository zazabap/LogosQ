// This file demonstrates the usage of the quantum computing library to create and manipulate basic quantum gates.

// use qforge::gates::{Gate, Pauli, Hadamard, CNOT};

fn main() {
    //     // Create a Pauli-X gate
    //     let pauli_x = Pauli::X;
    //     println!("Created gate: {:?}", pauli_x);

    //     // Create a Hadamard gate
    //     let hadamard = Hadamard;
    //     println!("Created gate: {:?}", hadamard);

    //     // Create a CNOT gate
    //     let cnot = CNOT::new(0, 1); // Control qubit 0, target qubit 1
    //     println!("Created gate: {:?}", cnot);

    //     // Example of applying gates to a quantum state can be added here
}
